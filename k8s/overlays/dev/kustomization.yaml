apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: auth-dev

resources:
  - namespace.yaml
  - ../../base

images:
  - name: your-dockerhub-username/jwt-auth-service
    newTag: local

patches:
  - patch: |-
      - op: replace
        path: /spec/replicas
        value: 1
    target:
      kind: Deployment
      name: jwt-auth-service

configMapGenerator:
  - name: auth-config
    behavior: merge
    literals:
      - NODE_ENV=development
      - APP_VERSION=dev
